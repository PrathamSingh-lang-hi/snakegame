# Snake Flask Project

Simple Flask-hosted Snake game.

Run locally:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Then open http://127.0.0.1:5000 in your browser.

will need to create an env to execute
